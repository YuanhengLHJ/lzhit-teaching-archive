---
name: lzhit-teaching-archive
description: 高校期末教学档案归档自动化技能（增强版）。当用户需要依据学校规范模板和课程教学大纲，把教师一学期的教学过程数据整理为学校要求的规范化存档材料时使用。支持 6 类课程模板自动识别与匹配（理论课考试 / 理论课考查 / 课程设计 / 实训 / 实习 + 课内实验），自动进行学生成绩一致性、材料完整性、格式合规性校验，并在数据冲突时依据模板向用户提问澄清；同时以高校教学专家视角对题目设计合理性、考试成绩分布、教学质量给出专业判断与建议。典型触发语：「整理期末归档」「按学校模板生成归档材料」「教学档案归档」「生成期末教学存档」「课程归档材料规范化」。注意：本技能与同名前缀不同的 teaching-archive 技能相互独立，本技能强调"依据学校规范模板 + 课程教学大纲"双输入驱动。
agent_created: true
---

# 高校期末教学档案归档自动化（lzhit-teaching-archive）

## Overview

本技能面向高校教师与教学秘书，把期末繁琐的「教学档案归档」工作自动化为一套结构化流程。与仅做目录打包的同类工具不同，本技能的核心是**双输入驱动**：

- **输入一**：教师提供的教学过程数据（成绩、考勤、试卷、作业样例等）
- **输入二**：课程教学大纲（用于识别课程类型、判断考核方式、确定归档要求）

输出为**学校要求的规范化存档材料**：依据学校提供的规范模板填充生成，并附带规范性校验报告与高校教学专家分析报告。

技能始终以**高校教学专家**角色运作：不仅完成材料生成，还对题目设计合理性、成绩分布形态、教学质量给出专业判断与改进建议。

## When to use

- 用户说「整理期末归档」「按学校模板生成归档材料」「教学档案归档」「生成期末教学存档」「课程归档材料规范化」等。
- 用户是高校教师或教学秘书，需要把一学期的教学材料按学校规范整理成可上交的存档。
- 用户手上有**学校下发的规范模板**（可能多个，按课程类型分类）和**课程教学大纲**。
- 用户希望生成材料后自动检查规范性（成绩一致性、材料完整性、格式合规性）。
- 用户希望获得教学质量的专业分析反馈。

> 若用户只需要简单的目录打包与占位符填充，且不涉及课程类型识别与规范性校验，可使用基础版 `teaching-archive` 技能。

## Workflow

### 第 1 步：收集学校规范模板

引导用户提供学校下发的存档材料模板。模板可能按课程类型分多个文件夹存放，本技能需要依据模板文件夹结构识别可用模板。

模板组织约定（详见 `assets/templates/README.md`）：

- 用户把模板放入一个**模板根目录**（可在技能配置中指定，或默认放入技能内 `assets/templates/`）。
- 模板按课程类型分子文件夹存放，子文件夹命名建议包含课程类型关键词，例如：
  - `理论课（考试）/`、`理论课（考查）/`、`课程设计/`、`实训/`、`实习/`
  - `课内实验/`（课内上机或课内实践，统称课内实验）
- 模板内可用 `{{课程名}}`、`{{学期}}`、`{{教师}}` 等占位符，引擎自动填充。
- 占位符必须完整位于同一个文本片段内，不要跨格式块拆分。

若用户尚未提供模板，技能先记录待补充项，不阻塞后续流程，但生成的归档材料会标记为「待补充」。

### 第 2 步：收集教学过程数据与课程教学大纲

引导用户提供两类输入：

- **课程教学大纲**（必需）：用于识别课程类型与考核方式。支持 `.docx`、`.txt`、`.md`、`.pdf`（文本层可提取）格式。技能会自动解析大纲中的「终结性考核」一栏及课程性质描述。
- **教学过程数据**（必需）：成绩、考勤、试卷、作业样例等。成绩/考勤类数据建议为 CSV 或 Excel，列名用中文（如 `学号`、`姓名`、`平时`、`期末`）。试卷、作业样例可为 docx/pdf/图片，按类别整理。

同时确认课程基本信息：课程名、课程代码、学期（如 `2025-2026-2`）、任课教师、班级、学院、专业。

### 第 3 步：识别课程类型并匹配模板

调用课程类型识别脚本（`scripts/identify_course_type.py`）解析大纲，判定课程类型。识别规则详见 `references/course-type-rules.md`，要点：

**5 类考核模板**（依据大纲「终结性考核」一栏判定）：

| 课程类型 | 大纲「终结性考核」关键词 | 典型场景 |
|----------|--------------------------|----------|
| 理论课（考试） | 考试 / 机试 / 笔试 / 闭卷考试 / 开卷考试 | 期末统一考试 |
| 理论课（考查） | 大作业 / 报告 / 论文 / 答辩 / 作品 | 非考试考查 |
| 课程设计 | 课程设计 / 设计任务 / 设计报告 | 工科设计课 |
| 实训 | 实训 / 实训报告 / 操作考核 | 集中实训 |
| 实习 | 实习 / 实习报告 / 实习鉴定 | 校外实习 |

> 关键判定：理论课（考试）与理论课（考查）的区别**仅看大纲「终结性考核」一栏**。出现「考试 / 机试 / 笔试」等即归为「理论课（考试）」；出现「大作业 / 报告」等即归为「理论课（考查）」。

**课内实验模板**（1 类，附加生成）：

当理论课（考试或考查）的大纲中存在「课内实验 / 上机 / 实践」环节时，**额外**根据课内实验模板生成对应的归档材料。课内实验归档材料是理论课归档材料的补充，不替代主归档材料。

**模板匹配策略**（详见 `references/template-discovery.md`）：

> ⚠️ **模板可能随学期/学院变化，技能不写死任何模板内容**。所有模板信息通过运行时动态扫描模板目录获得。

- 用户可能一次性提供多个课程类型的模板文件夹（典型组织：`1.理论课考试.../`、`2.理论课考查.../`、...、`7.补考/`）。
- 技能**动态扫描**模板根目录的一级子目录，按子目录名关键词匹配识别出的课程类型（如"理论课"+"考试"→T1）。
- 进入匹配子目录后，识别三类角色文件：
  1. **归档确认单**（文件名含"归档确认单.xlsx"）→ 校验依据（核心）。
  2. **要求及材料提交要求文件**（文件名含"要求"+"提交"或以"！！"开头）→ 校验依据补充。
  3. **附件模板**（"模板（XXX）"子目录下，文件名以"附件"开头）→ 生成依据。
- 解析附件文件名 `附件N：材料名（要求说明）.扩展名`，提取材料名与要求说明。
- 文件过滤：跳过 `~$` 锁文件、`.jpg/.png` 等图片示例（不解析，节省 token）。
- 若有多个候选子目录，向用户确认；若未找到匹配，提示用户补充对应类型模板。

### 第 4 步：生成规范化存档材料并校验

调用主引擎（`scripts/build_archive.py`）：

1. 依据课程类型加载对应的归档规范（材料清单、文件命名、生成方式）。
2. 填充模板占位符，生成 docx/xlsx 材料。
3. 由教学过程数据生成成绩表、考勤表等数据驱动文档。
4. 若识别出课内实验环节，额外生成课内实验归档材料。
5. 在**每份生成材料的末尾**追加附注：
   > 本材料由AI辅助生成，仅供参考，请认真核对后由相关负责人签字确认。
6. 调用规范性校验（`scripts/validate_archive.py`）生成校验报告：
   - 学生成绩前后一致性（平时+期末按权重 = 总评；成绩登记表 vs 试卷成绩一致）
   - 材料完整性（对照模板清单，必交材料齐全）
   - 格式合规性（文件命名规范、占位符已全部填充无残留）
   - 异常项逐条列出。
7. 调用专家分析（`scripts/expert_report.py`）生成教学专家分析报告。
8. 产出归档目录树、归档清单（md + csv）、规范性校验报告、专家分析报告，并打包 ZIP。

### 材料处理边界与询问（关键业务规则）

技能**不处理**部分材料，须在生成前明确告知用户不处理范围（详见 `references/business-rules.md` 第二节）：

- **理论课（考试）T1 不生成**：命题计划表、A/B 样卷、答题纸（含封面）、标准答案及评分细则、试卷审批表、考试资格名单认定及考场签到表、学生答题纸。这些属命题/阅卷/考务环节，由任课教师与教务秘书处理。技能**明确告知用户**这些不处理。
- **理论课（考查）T2《课程考核方案》**：默认不生成，须**询问用户**是否已有；用户没有时再生成。
- **任务书/实验指导书/实训指导书/实习指导书**：判断教师是否可能具备；**不具备时询问用户**是否需要技能生成；用户确认后再生成。

### 补考名单生成（仅 T1）

仅理论课（考试）课程生成《补考名单》（对应模板 `7.补考/` 目录与 T1 模板下的"附件9：补考名单"）。补考资格判定（详见 `references/business-rules.md` 第一节）：

- **有补考资格**：参加了考试（卷面 ≠ 0 且未标记"缺考"）且综合成绩 < 60。
- **无补考资格**：缺考（卷面为 0 且标记"缺考"），或综合成绩 ≥ 60。
- **需向用户确认**：备注含"违纪""免考""缓考""特殊"等非"缺考"标记且综合 < 60。

补考名单生成后须提示用户核实，最终以教务秘书确认为准。

### 冲突处理与提问澄清

当用户提供的教学过程数据存在冲突或混乱时（如成绩总评与平时+期末加权不一致、数据缺失、模板字段对不上），技能**依据模板字段定义向用户提问澄清**，而非擅自臆测。提问要点：

- 指明冲突字段、冲突位置、模板要求值与实际值的差异。
- 给出可选解决方案（如：以哪一方为准、是否需要补充数据、是否调整权重）。
- 一次提问不超过 3 个核心冲突，避免淹没用户。

详见 `references/validation-rules.md`。

## 专业分析框架

技能始终以**高校教学专家**角色，对以下维度给出专业判断与建议（详见 `references/expert-analysis.md`）：

- **题目设计合理性**：覆盖度（与大纲知识点契合度）、难度分布（易/中/难比例）、题型搭配、分值合理性。
- **考试成绩分布**：均值、标准差、及格率、优秀率、分布形态（正态/偏态/双峰）、异常分数识别。
- **教学质量判断**：基于成绩分布与试卷分析推断教学效果，给出针对性改进建议（如调整教学重点、改进考核方式、加强薄弱环节）。

分析报告独立于归档材料，作为教学反思与质量改进的参考。

## Resources

### scripts/build_archive.py

主引擎。读取配置与归档规范，完成占位符填充、数据表生成、目录组织、清单产出、ZIP 打包，并串联课程类型识别、规范性校验、专家分析。依赖 `openpyxl`、`python-docx`（见 `scripts/requirements.txt`）。

### scripts/identify_course_type.py

课程类型识别。读取大纲文件，提取「终结性考核」一栏及课程性质描述，按关键词匹配判定 5 类考核类型之一，并检测是否存在课内实验环节。返回课程类型、是否需要课内实验归档、识别证据（命中的关键词与原文片段）。

### scripts/validate_archive.py

规范性校验。检查成绩一致性、材料完整性、格式合规性，输出 `规范性校验报告.md`，逐项标注通过/异常/待补充。

### scripts/expert_report.py

专家分析报告生成。基于成绩数据与试卷分析，按 `references/expert-analysis.md` 框架输出题目设计、成绩分布、教学质量三维度分析。

### references/course-type-rules.md

**课程类型识别规则的权威说明**：6 类课程类型的定义、关键词、大纲字段对照、识别流程、边界情况处理。**待补充**：课程类型与归档要求的具体对应关系（每类课程具体需要哪些归档材料），将在后续对话中补充。

### references/archive-convention.md

**归档规范与模板匹配说明**：模板组织结构、子文件夹命名约定、模板匹配优先级、占位符清单、输出目录结构、文件命名规则。

### references/business-rules.md

**业务规则手册（核心）**：集中沉淀全部业务知识——补考规则（仅 T1，参加考试且挂科）、材料处理边界（T1 不生成的试卷类材料、T2 课程考核方案询问、指导书类判断教师是否具备）、校验依据来源（归档确认单+要求文件）、模板读取策略（不写死、跳过图片）、成绩多 Sheet 结构理解。**新增业务知识优先补充到本文件**。

### references/template-discovery.md

**模板动态发现策略**：规定如何动态扫描模板目录、按关键词匹配课程类型子目录、识别归档确认单/要求文件/附件模板三类角色文件、解析附件文件名、过滤图片与临时文件、模板缺失兜底。**确保模板变化时无需改代码**。

### references/validation-rules.md

**规范性校验规则的权威说明**：成绩一致性校验规则、材料完整性校验清单、格式合规性校验项、异常处理与冲突提问流程。**待补充**：规范性校验的详细规则（部分校验阈值与容错策略），将在后续对话中补充。

### references/expert-analysis.md

**高校教学专家分析框架**：题目设计合理性分析维度、成绩分布分析方法、教学质量判断维度、建议生成原则、报告输出格式。

### references/placeholders.md

**占位符字段对照表**：所有可用占位符的清单、含义、来源、是否必需。

### assets/templates/README.md

**模板放置指引**：模板根目录组织方式、按课程类型分子文件夹、命名规则、占位符写法、多模板匹配策略、缺少模板时的兜底行为。

## 可扩展性

本技能设计为可持续完善。以下内容标记为「待补充」，将在后续对话中逐步填充，无需重写整个 skill：

- `references/course-type-rules.md` 中的「课程类型与归档要求的具体对应关系」。
- `references/validation-rules.md` 中的「规范性校验的详细规则」。
- `references/archive-convention.md` 中可由用户自定义学院专属规范（覆盖默认）。

补充方式：直接编辑对应 references 文件，或与用户对话确认后由技能维护者更新。脚本引擎读取 references 中的规则，因此补充规则后无需改代码即可生效。

## 自定义学院规范

若用户的学院有专属归档规范，编辑 `references/archive-convention.md`（说明）与对应的规则文件：

- 修改课程类型对应的归档材料清单。
- 调整文件命名规则。
- 调整成绩权重（默认平时 40% + 期末 60%，可在配置中覆盖）。

改完保存，重新运行第 4 步即可。

两者相互独立，可同时安装。
